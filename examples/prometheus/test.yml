scrape_configs:
  - job_name: 'all-smi-node'

    # Override the global default and scrape targets from this job every 5 seconds.
    scrape_interval: 5s

    static_configs:
      - targets: ['localhost:8083']
        labels:
          group: 'production'
